<?xml version="1.0" encoding="utf-8"?>

<!-- Manifest is a directory that lists all the components that are used in this app-->
<!--The manifest file describes essential information about application-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Change an Android App’s Name?-->
    <!--Go to the app > manifests > AndroidManifest.xml file and change the android:label field in your application node in AndroidManifest.xml.-->
    <!--Then Go to the app > res > values > strings.xml file to change the name of App-->
    <!--a new comment to github-->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicApp"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>